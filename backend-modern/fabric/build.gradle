plugins {
    id 'multiloader-loader'
    id 'fabric-loom'
}

configurations {
    compileClasspath.extendsFrom commonJava
    runtimeClasspath.extendsFrom commonResources
}

dependencies {
    minecraft "com.mojang:minecraft:${minecraft_version}"

    mappings "net.fabricmc:yarn:${yarn_mappings}:v2"

    modImplementation "net.fabricmc:fabric-loader:${fabric_loader_version}"
    modImplementation "net.fabricmc.fabric-api:fabric-api:${fabric_version}"

    commonJava project(path: ":backend-modern:common", configuration: "commonJava")
    commonResources project(path: ":backend-modern:common", configuration: "commonResources")

    // networking api
    implementation project(':isekai-api')
}

loom {
    def aw = project(':backend-modern:common').file("src/main/resources/${mod_id}.accesswidener")
    if (aw.exists()) {
        accessWidenerPath.set(aw)
    }
    mixin {
        defaultRefmapName.set("${mod_id}.refmap.json")
    }
    runs {
        client {
            client()
            setConfigName('Fabric Client')
            ideConfigGenerated(true)
            runDir('runs/client')
        }
        server {
            server()
            setConfigName('Fabric Server')
            ideConfigGenerated(true)
            runDir('runs/server')
        }
    }
}