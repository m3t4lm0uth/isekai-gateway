# more heap for Forge/NeoForge mapping tasks
org.gradle.jvmargs=-Xmx5G -XX:+UseG1GC

# enable daemon for multi-module builds
org.gradle.daemon=true

# (Optional) Limit parallel workers if memory is tight
# org.gradle.workers.max=2
